<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:alpha="6"
    android:background="#a0000000"
    android:orientation="vertical">
    <ImageButton
        android:id="@+id/cancel_gallery_image_show"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_marginRight="30dp"
        android:layout_marginEnd="30dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_above="@+id/gallery_selected_image_preview"
        android:background="@android:color/transparent"
        android:src="@drawable/x"/>
    <ImageView
        android:id="@+id/gallery_selected_image_preview"
        android:layout_margin="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:transitionName="galleryimage"
        android:layout_centerInParent="true"
        android:src="@drawable/sample_image_teacher"
        xmlns:android="http://schemas.android.com/apk/res/android"
        tools:targetApi="lollipop"
        tools:ignore="ContentDescription" />
    <ZoomControls
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:id="@+id/zc1"/>
</RelativeLayout>